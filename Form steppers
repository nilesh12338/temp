<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Survey Wizard Form</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/jquery-validation@1.19.5/dist/jquery.validate.min.js"></script>
  <style>
    .step {
      position: absolute;
      width: 100%;
      opacity: 0;
      transform: translateX(100%);
      transition: all 0.5s ease;
    }
    .step.active {
      opacity: 1;
      transform: translateX(0);
      position: relative;
    }
    .step.slide-out-left { transform: translateX(-100%); opacity: 0; }
    .step.slide-out-right { transform: translateX(100%); opacity: 0; }
    .form-container { position: relative; min-height: 220px; overflow: hidden; }
    #cityList { z-index: 10; }
    #summary { display:none; }
  </style>
</head>
<body class="bg-light d-flex align-items-center justify-content-center vh-100">

<div class="card shadow-lg p-4" style="width:480px;">
  <h4 class="mb-3">Survey Form</h4>
  <div id="progressText" class="mb-3 fw-bold">Step 1 of 6</div>

  <form id="surveyForm">
    <div class="form-container">

      <!-- Step 1 -->
      <div class="step active" id="step1">
        <label class="form-label">1. Choose your gender</label><br>
        <div class="form-check">
          <input type="radio" name="gender" value="Male" class="form-check-input" required> Male
        </div>
        <div class="form-check">
          <input type="radio" name="gender" value="Female" class="form-check-input"> Female
        </div>
      </div>

      <!-- Step 2 -->
      <div class="step" id="step2">
        <label class="form-label">2. Enter your city</label>
        <input type="text" name="city" id="city" class="form-control" placeholder="Start typing city..." required>
        <div id="cityList" class="list-group position-absolute w-100"></div>
      </div>

      <!-- Step 3 -->
      <div class="step" id="step3">
        <label class="form-label">3. Enter monthly income</label>
        <input type="number" name="income" id="income" class="form-control" placeholder="Enter amount" required min="1">
      </div>

      <!-- Step 4 -->
      <div class="step" id="step4">
        <label class="form-label">4. Choose your qualification</label>
        <select name="qualification" id="qualification" class="form-select" required>
          <option value="">-- Select --</option>
          <option value="Graduate">Graduate</option>
          <option value="Post Graduate">Post Graduate</option>
          <option value="Doctorate">Doctorate</option>
        </select>

        <!-- Graduate fields -->
        <div id="graduateFields" class="mt-3" style="display:none;">
          <label class="form-label">4(a). College Name</label>
          <input type="text" name="collegeName" class="form-control">

          <label class="form-label mt-2">4(b). Year of Passout</label>
          <input type="number" name="passoutYear" class="form-control" min="1900" max="2099">
        </div>

        <!-- Post Graduate fields -->
        <div id="postGraduateFields" class="mt-3" style="display:none;">
          <label class="form-label">4(a). University</label>
          <input type="text" name="university" class="form-control">

          <label class="form-label mt-2">4(b). Specialization</label>
          <input type="text" name="specialization" class="form-control">
        </div>
      </div>

      <!-- Step 5 -->
      <div class="step" id="step5">
        <label class="form-label">5. Do you agree with terms?</label><br>
        <div class="form-check">
          <input type="radio" name="terms" value="Yes" class="form-check-input" required> Yes
        </div>
        <div class="form-check">
          <input type="radio" name="terms" value="No" class="form-check-input"> No
        </div>
      </div>

      <!-- Step 6 (Summary placeholder) -->
      <div class="step" id="step6">
        <div id="summary" class="text-center">
          <h5 class="text-success">🎉 Congratulations!</h5>
          <p>Your responses have been submitted.</p>
          <div id="summaryData" class="text-start mt-3"></div>
        </div>
      </div>
    </div>

    <!-- Navigation -->
    <div class="d-flex justify-content-between mt-4">
      <button type="button" class="btn btn-secondary" id="prevBtn" disabled>Back</button>
      <div>
        <button type="button" class="btn btn-outline-secondary me-2" id="skipBtn">Skip</button>
        <button type="button" class="btn btn-primary" id="nextBtn">Next</button>
      </div>
    </div>
  </form>
</div>

<script>
$(function(){
  let currentStep = 1;
  const totalSteps = 6;

  $("#surveyForm").validate({
    errorClass: "text-danger",
    errorPlacement: function(error, element){
      if(element.attr("type") === "radio"){
        error.insertAfter(element.closest("div.form-check").parent());
      } else {
        error.insertAfter(element);
      }
    }
  });

  function updateProgress() {
    $("#progressText").text("Step " + currentStep + " of " + totalSteps);
  }

  function showStep(newStep, direction){
    const $current = $("#step" + currentStep);
    const $next = $("#step" + newStep);

    if(direction === "forward"){
      $current.removeClass("active").addClass("slide-out-left");
    } else {
      $current.removeClass("active").addClass("slide-out-right");
    }
    setTimeout(() => { $current.removeClass("slide-out-left slide-out-right"); }, 500);

    $next.addClass("active");
    currentStep = newStep;
    updateProgress();

    $("#prevBtn").prop("disabled", currentStep === 1);
    $("#nextBtn").text(currentStep === totalSteps ? "Submit" : "Next");
  }

  $("#nextBtn").click(function(){
    if(currentStep < totalSteps){
      if(!$("#surveyForm").valid()) return;
      if(currentStep === totalSteps-1){
        // Before going to summary, build summary
        let data = $("#surveyForm").serializeArray();
        let html = "<ul class='list-group'>";
        data.forEach(item => {
          html += `<li class='list-group-item'><strong>${item.name}:</strong> ${item.value}</li>`;
        });
        html += "</ul>";
        $("#summaryData").html(html);
        $("#summary").show();
      }
      showStep(currentStep+1, "forward");
    } else {
      alert("Form submitted successfully!");
      console.log($("#surveyForm").serializeArray());
    }
  });

  $("#prevBtn").click(function(){
    if(currentStep > 1){
      showStep(currentStep-1, "backward");
    }
  });

  $("#skipBtn").click(function(){
    if(currentStep < totalSteps){
      showStep(currentStep+1, "forward");
    }
  });

  // Qualification conditional fields
  $("#qualification").change(function(){
    let val = $(this).val();
    $("#graduateFields, #postGraduateFields").hide();
    if(val === "Graduate") $("#graduateFields").show();
    if(val === "Post Graduate") $("#postGraduateFields").show();
  });

  // Dummy autocomplete cities
  const cities = ["Mumbai","Delhi","Bangalore","Pune","Chennai","Kolkata"];
  $("#city").on("input", function(){
    let val = $(this).val().toLowerCase();
    let matches = cities.filter(c => c.toLowerCase().includes(val));
    $("#cityList").empty();
    if(val){
      matches.forEach(c => {
        $("#cityList").append(`<a href="#" class="list-group-item list-group-item-action">${c}</a>`);
      });
    }
  });
  $(document).on("click", "#cityList a", function(e){
    e.preventDefault();
    $("#city").val($(this).text());
    $("#cityList").empty();
  });

  updateProgress();
});
</script>
</body>
</html>
