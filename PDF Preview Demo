<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>PDF Preview Demo</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Bootstrap 5.3.0 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light p-4">

  <div class="container">
    <h3 class="mb-4 text-primary">PDF Preview Demo (Server File Fetch)</h3>

    <!-- List of PDF Files -->
    <ul id="pdfList" class="list-group">
      <li class="list-group-item">
        <a href="#" onclick="loadPDF('reports/sample1.pdf')">Sample Report 1</a>
      </li>
      <li class="list-group-item">
        <a href="#" onclick="loadPDF('reports/sample2.pdf')">Sample Report 2</a>
      </li>
      <li class="list-group-item">
        <a href="#" onclick="loadPDF('reports/sample3.pdf')">Sample Report 3</a>
      </li>
    </ul>
  </div>

  <!-- PDF Preview Modal -->
  <div class="modal fade" id="pdfModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-xl">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title">PDF Preview</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
        </div>
        <div class="modal-body p-0">
          <iframe id="pdfViewer" src="" style="width:100%; height:80vh; border:none;"></iframe>
        </div>
        <div class="modal-footer">
          <a id="downloadLink" href="#" class="btn btn-primary" download>Download</a>
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS + Popper -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

  <script>
    async function loadPDF(filePath) {
      try {
        // Fetch PDF as blob (from server)
        const response = await fetch(filePath);
        if (!response.ok) throw new Error('File not found');

        const blob = await response.blob();

        // Create a temporary object URL
        const pdfUrl = URL.createObjectURL(blob);

        // Show PDF in iframe
        document.getElementById('pdfViewer').src = pdfUrl;

        // Set download link
        const fileName = filePath.split('/').pop();
        const downloadLink = document.getElementById('downloadLink');
        downloadLink.href = pdfUrl;
        downloadLink.download = fileName;

        // Show modal
        const modal = new bootstrap.Modal(document.getElementById('pdfModal'));
        modal.show();
      } catch (error) {
        alert('Unable to load PDF: ' + error.message);
      }
    }
  </script>
</body>
</html>
