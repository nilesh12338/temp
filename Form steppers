<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Sliding Survey Form</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/jquery-validation@1.19.5/dist/jquery.validate.min.js"></script>
  <style>
    .step {
      position: absolute;
      width: 100%;
      opacity: 0;
      transform: translateX(100%);
      transition: all 0.5s ease;
    }
    .step.active {
      opacity: 1;
      transform: translateX(0);
      position: relative;
    }
    .step.slide-out-left { transform: translateX(-100%); opacity: 0; }
    .step.slide-out-right { transform: translateX(100%); opacity: 0; }
    .form-container { position: relative; min-height: 240px; }
    #cityList { z-index: 10; }
    #summary { display: none; }
  </style>
</head>
<body class="bg-light d-flex align-items-center justify-content-center vh-100">

<div class="card shadow-lg p-4" style="width:500px;">
  <h4 class="mb-3 text-center">Survey Form</h4>
  <div class="progress mb-3">
    <div id="progressBar" class="progress-bar" role="progressbar" style="width: 20%;">Step 1 of 5</div>
  </div>

  <form id="surveyForm">
    <div class="form-container">

      <!-- Step 1 -->
      <div class="step active" id="step1">
        <label class="form-label">1. Choose your gender</label><br>
        <div class="form-check">
          <input type="radio" name="gender" value="Male" class="form-check-input" required> Male
        </div>
        <div class="form-check">
          <input type="radio" name="gender" value="Female" class="form-check-input"> Female
        </div>
      </div>

      <!-- Step 2 -->
      <div class="step" id="step2">
        <label class="form-label">2. Enter your city (autocomplete)</label>
        <input type="text" name="city" id="city" class="form-control" placeholder="Start typing city..." required>
        <div id="cityList" class="list-group position-absolute w-100"></div>
      </div>

      <!-- Step 3 -->
      <div class="step" id="step3">
        <label class="form-label">3. Enter monthly income (amount)</label>
        <input type="number" name="income" id="income" class="form-control" placeholder="Enter amount" required>
      </div>

      <!-- Step 4 -->
      <div class="step" id="step4">
        <label class="form-label">4. Choose your qualification</label>
        <select name="qualification" id="qualification" class="form-select" required>
          <option value="">-- Select --</option>
          <option value="Graduate">Graduate</option>
          <option value="Post Graduate">Post Graduate</option>
          <option value="Doctorate">Doctorate</option>
        </select>

        <!-- Graduate Sub Questions -->
        <div id="graduateFields" style="display:none;" class="mt-3">
          <label class="form-label">4(a). College Name</label>
          <input type="text" name="collegeName" class="form-control">
          <label class="form-label mt-2">4(b). Year of Passout</label>
          <input type="number" name="passoutYear" class="form-control" min="1900" max="2099">
        </div>

        <!-- Post Graduate Sub Questions -->
        <div id="postGraduateFields" style="display:none;" class="mt-3">
          <label class="form-label">4(a). University Name</label>
          <input type="text" name="pgUniversity" class="form-control">
          <label class="form-label mt-2">4(b). Specialization</label>
          <input type="text" name="pgSpecialization" class="form-control">
        </div>
      </div>

      <!-- Step 5 -->
      <div class="step" id="step5">
        <label class="form-label">5. Do you agree with terms?</label><br>
        <div class="form-check">
          <input type="radio" name="terms" value="Yes" class="form-check-input" required> Yes
        </div>
        <div class="form-check">
          <input type="radio" name="terms" value="No" class="form-check-input"> No
        </div>
      </div>

      <!-- Summary -->
      <div class="step" id="summary">
        <h5 class="text-success">🎉 Congratulations!</h5>
        <p>Your responses have been recorded.</p>
        <div id="summaryContent" class="mt-3"></div>
      </div>
    </div>

    <!-- Navigation -->
    <div class="d-flex justify-content-between mt-4">
      <button type="button" class="btn btn-secondary" id="prevBtn" disabled>Back</button>
      <div>
        <button type="button" class="btn btn-warning" id="skipBtn">Skip</button>
        <button type="button" class="btn btn-primary" id="nextBtn">Next</button>
      </div>
    </div>
  </form>
</div>

<script>
$(function(){
  let currentStep = 1;
  const totalSteps = 5;

  // jQuery Validation
  $("#surveyForm").validate({ errorClass: "text-danger" });

  function updateProgress() {
    let percent = (currentStep / totalSteps) * 100;
    $("#progressBar").css("width", percent+"%").text("Step " + currentStep + " of " + totalSteps);
  }

  function showStep(newStep, direction) {
    const $current = $("#step" + currentStep);
    const $next = $("#step" + newStep);

    if(direction === "forward"){
      $current.removeClass("active").addClass("slide-out-left");
    } else {
      $current.removeClass("active").addClass("slide-out-right");
    }

    setTimeout(() => { $current.removeClass("slide-out-left slide-out-right"); }, 500);
    $next.addClass("active");
  }

  function handleNext(skip=false) {
    if(!skip && !$("#surveyForm").valid()) return;

    if(currentStep < totalSteps){
      let newStep = currentStep + 1;
      showStep(newStep, "forward");
      currentStep = newStep;
      updateProgress();
    } else {
      // Show summary
      $("#progressBar").css("width","100%").text("Completed");
      $("#summary").addClass("active");

      let data = $("#surveyForm").serializeArray();
      let html = "<ul class='list-group'>";
      data.forEach(d => { html += `<li class='list-group-item'><strong>${d.name}:</strong> ${d.value}</li>`; });
      html += "</ul>";
      $("#summaryContent").html(html);

      console.log("Form Data:", data);
      $("#nextBtn, #prevBtn, #skipBtn").hide();
    }
    $("#prevBtn").prop("disabled", currentStep === 1);
    $("#nextBtn").text(currentStep === totalSteps ? "Submit" : "Next");
  }

  $("#nextBtn").click(()=> handleNext(false));
  $("#skipBtn").click(()=> handleNext(true));
  $("#prevBtn").click(function(){
    if(currentStep > 1){
      let newStep = currentStep - 1;
      showStep(newStep, "backward");
      currentStep = newStep;
      updateProgress();
    }
    $("#prevBtn").prop("disabled", currentStep === 1);
    $("#nextBtn").text("Next");
  });

  // Autocomplete dummy
  const cities = ["Mumbai","Delhi","Bangalore","Pune","Chennai","Kolkata"];
  $("#city").on("input", function(){
    let val = $(this).val().toLowerCase();
    let matches = cities.filter(c => c.toLowerCase().includes(val));
    $("#cityList").empty();
    if(val){
      matches.forEach(c => $("#cityList").append(`<a href="#" class="list-group-item list-group-item-action">${c}</a>`));
    }
  });
  $(document).on("click", "#cityList a", function(e){
    e.preventDefault(); $("#city").val($(this).text()); $("#cityList").empty();
  });

  // Qualification sub-questions
  $("#qualification").change(function(){
    $("#graduateFields, #postGraduateFields").hide();
    if($(this).val() === "Graduate") $("#graduateFields").show();
    if($(this).val() === "Post Graduate") $("#postGraduateFields").show();
  });
});
</script>
</body>
</html>
