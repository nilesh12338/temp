<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>API Dashboard – Download Selected APIs</title>

  <!-- Bootstrap 5.3 -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

  <!-- JSZip + FileSaver -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/jszip/3.7.1/jszip.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/FileSaver.js/2.0.5/FileSaver.min.js"></script>

  <style>
    body { background-color: #f8f9fa; padding: 2rem; }
    .card { margin-bottom: 1rem; }

    /* Modern translucent loader overlay */
    .loader-overlay {
      display: none;
      position: fixed;
      top: 0; left: 0; right: 0; bottom: 0;
      background: rgba(255, 255, 255, 0.7);
      backdrop-filter: blur(8px);
      z-index: 1055;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      text-align: center;
    }

    .dot-loader {
      display: inline-block;
      font-weight: 600;
      font-size: 1.2rem;
      color: #0d6efd;
    }

    .dot-loader span {
      display: inline-block;
      animation: blink 1.4s infinite both;
    }

    .dot-loader span:nth-child(2) { animation-delay: .2s; }
    .dot-loader span:nth-child(3) { animation-delay: .4s; }

    @keyframes blink {
      0%, 80%, 100% { opacity: 0; }
      40% { opacity: 1; }
    }

    .loader-text {
      font-weight: 500;
      font-size: 1.1rem;
      color: #333;
      margin-top: 10px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2 class="mb-4 text-center">📦 Project API Dashboard</h2>

    <div class="card shadow-sm">
      <div class="card-body">
        <div class="d-flex justify-content-between align-items-center mb-3">
          <h5>Select Projects</h5>
          <button id="downloadBtn" class="btn btn-primary">
            <i class="bi bi-download me-1"></i> Download Selected APIs
          </button>
        </div>

        <div id="projectList" class="list-group mb-3">
          <label class="list-group-item">
            <input class="form-check-input me-1" type="checkbox" value="https://petstore.swagger.io/v2/swagger.json" data-name="Petstore"> Project 1 – Petstore API
          </label>
          <label class="list-group-item">
            <input class="form-check-input me-1" type="checkbox" value="https://api.apis.guru/v2/specs/github.com/1.1.4/swagger.json" data-name="GitHubAPI"> Project 2 – GitHub API
          </label>
          <label class="list-group-item">
            <input class="form-check-input me-1" type="checkbox" value="https://api.apis.guru/v2/specs/azure.com/1.0.0/swagger.json" data-name="AzureAPI"> Project 3 – Azure API
          </label>
        </div>
      </div>
    </div>
  </div>

  <!-- Modern Loader Overlay -->
  <div class="loader-overlay d-flex" id="loaderOverlay">
    <div>
      <div class="spinner-border text-primary mb-3" role="status" style="width: 3rem; height: 3rem;">
        <span class="visually-hidden">Loading...</span>
      </div>
      <div class="dot-loader">Downloading<span>.</span><span>.</span><span>.</span></div>
      <div id="loaderText" class="loader-text mt-2">Please wait while we fetch APIs</div>
    </div>
  </div>

  <!-- Toast Notification -->
  <div class="toast-container position-fixed bottom-0 end-0 p-3">
    <div id="toastMsg" class="toast text-bg-success border-0" role="alert" aria-live="assertive" aria-atomic="true">
      <div class="d-flex">
        <div class="toast-body"></div>
        <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast"></button>
      </div>
    </div>
  </div>

  <script>
    const btn = document.getElementById('downloadBtn');
    const loader = document.getElementById('loaderOverlay');
    const loaderText = document.getElementById('loaderText');
    const toastEl = document.getElementById('toastMsg');
    const toastBody = toastEl.querySelector('.toast-body');
    const toast = new bootstrap.Toast(toastEl);

    btn.addEventListener('click', async () => {
      const selected = [...document.querySelectorAll('#projectList input:checked')];
      if (!selected.length) {
        showToast('Please select at least one project!', true);
        return;
      }

      btn.disabled = true;
      loader.style.display = 'flex';
      loaderText.textContent = 'Preparing API downloads...';

      const zip = new JSZip();
      const total = selected.length;

      for (let i = 0; i < total; i++) {
        const item = selected[i];
        const apiUrl = item.value;
        const name = item.dataset.name;
        loaderText.textContent = `Downloading ${name} (${i + 1} of ${total})...`;

        try {
          const res = await fetch(apiUrl);
          const data = await res.text();
          zip.file(`${name}.json`, data);
        } catch (err) {
          zip.file(`${name}_error.txt`, `Failed to fetch ${apiUrl}\n${err}`);
        }
      }

      loaderText.textContent = 'Compressing files...';
      const content = await zip.generateAsync({ type: 'blob' });
      saveAs(content, 'Selected_Project_APIs.zip');

      loader.style.display = 'none';
      btn.disabled = false;
      showToast('All selected APIs downloaded successfully!');
    });

    function showToast(message, isError = false) {
      toastBody.textContent = message;
      toastEl.classList.toggle('text-bg-danger', isError);
      toastEl.classList.toggle('text-bg-success', !isError);
      toast.show();
    }
  </script>
</body>
</html>
